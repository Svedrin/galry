---
kind: pipeline
name: default

steps:
- name: build for Linux
  image: rust:alpine
  environment:
    RUSTFLAGS: -D warnings
  commands:
    - apk add --no-cache musl-dev
    - rustup install nightly
    - cargo +nightly check --release
    - cargo +nightly build --release

- name: build docker image
  image: plugins/docker
  settings:
    repo: svedrin/galry
    username: svedrin
    password:
      from_secret: docker-registry-pw
  when:
    event:
      exclude:
      - pull_request

---
kind: signature
hmac: 4e8587f3a4607d12687ac851ca88a398fa5c89eb592cd2189beebeb058401b1b

...
