---
kind: pipeline
name: default

steps:
- name: build for Linux
  image: rust:alpine
  environment:
    RUSTFLAGS: -D warnings
  commands:
    - apk add --no-cache musl-dev
    - rustup install nightly
    - cargo +nightly check --release
    - cargo +nightly build --release
    - cp -p target/release/galry target/release/galry.tmp
    - mv -f target/release/galry.tmp target/release/galry

- name: build docker image
  image: plugins/docker
  settings:
    repo: svedrin/galry
    username: svedrin
    password:
      from_secret: docker-registry-pw
    cache_from: "svedrin/galry:latest"
  when:
    event:
      exclude:
      - pull_request

---
kind: signature
hmac: 54f12309bc7b3c6474a02566e05d7ac799d9944646a61b181f48f5fee3182aa2

...
